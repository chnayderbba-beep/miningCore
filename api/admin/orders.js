const {json}=require("../../lib/http"); const {getPool}=require("../../lib/db"); const {requireUser}=require("../../lib/auth");
module.exports=async(req,res)=>{const u=await requireUser(req);if(!u)return json(res,401,{error:"Unauthorized"});if(u.role!=="admin")return json(res,403,{error:"Forbidden"});if(req.method!=="GET")return json(res,405,{error:"Method not allowed"});const {rows}=await getPool().query("SELECT o.*,u.email,p.name plan_name FROM orders o JOIN users u ON u.id=o.user_id JOIN plans p ON p.id=o.plan_id ORDER BY o.created_at DESC LIMIT 500");return json(res,200,{orders:rows});};
