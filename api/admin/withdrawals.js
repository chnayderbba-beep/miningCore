const {json}=require("../../lib/http"); const {getPool}=require("../../lib/db"); const {requireUser}=require("../../lib/auth");
module.exports=async(req,res)=>{const u=await requireUser(req);if(!u)return json(res,401,{error:"Unauthorized"});if(u.role!=="admin")return json(res,403,{error:"Forbidden"});const {rows}=await getPool().query("SELECT w.*,u.email FROM withdrawals w JOIN users u ON u.id=w.user_id ORDER BY requested_at DESC LIMIT 500");return json(res,200,{withdrawals:rows});};
