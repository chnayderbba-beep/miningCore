const {json}=require("../lib/http"); const {getPool}=require("../lib/db"); const {requireUser}=require("../lib/auth");
module.exports=async(req,res)=>{const u=await requireUser(req);if(!u)return json(res,401,{error:"Unauthorized"});const {rows}=await getPool().query(`SELECT currency,COALESCE(SUM(CASE WHEN type IN ('MINING_CREDIT','ADJUSTMENT','WITHDRAWAL_REVERSED') THEN amount WHEN type IN ('WITHDRAWAL_RESERVE','WITHDRAWAL_FEE','WITHDRAWAL_COMPLETED') THEN -amount ELSE 0 END),0)::text available FROM wallet_ledger WHERE user_id=$1 GROUP BY currency`,[u.id]);return json(res,200,{balances:rows});};
