const {json}=require("../../http"); const {getPool}=require("../../db"); const {requireUser}=require("../../auth");
module.exports=async(req,res)=>{const u=await requireUser(req);if(!u)return json(res,401,{error:"Unauthorized"});if(u.role!=="admin")return json(res,403,{error:"Forbidden"});if(req.method!=="GET")return json(res,405,{error:"Method not allowed"});const {rows}=await getPool().query("SELECT id,full_name,username,email,role,status,created_at FROM users ORDER BY created_at DESC LIMIT 1000");return json(res,200,{users:rows});};
